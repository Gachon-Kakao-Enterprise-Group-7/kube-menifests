apiVersion: v1
kind: Service
metadata:
  labels:
    app: libertyform-react
  name: libertyform-react
spec:
  type: LoadBalancer
  selector:
    app: libertyform-react
  ports:
    - port: 5000
      targetPort: 5000
