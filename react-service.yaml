apiVersion: v1
kind: Service
metadata:
  labels:
    app: libertyform-react
  name: libertyform-react
spec:
  type: LoadBalancer
  selector:
    app: libertyform-react
  ports:
    - port: 8080
      targetPort: 8080
